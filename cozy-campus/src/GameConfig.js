/*
 * Chave única de debug do projeto.
 *
 * true:  painéis de personagens/performance, NavigationGraphHelper e
 *        câmera bird's-eye são habilitados.
 * false: todos esses recursos ficam desabilitados e a câmera usa a vista
 *        normal do jogo.
 */
export const DEBUG_MODE = true;

/*
 * Painel compacto e independente do modo debug completo.
 * Ele não exibe o grafo, não altera a câmera e não abre Character Debug.
 */
export const SIMPLE_PERFORMANCE_DEBUG = false;
